version: 2

sources:
  - name: silver_transport
    database: "{{ target.database }}"
    schema: "{{ env_var('DBT_SILVER_SCHEMA', 'SILVER_CANONICAL') }}"
    tables:
      - name: vehicle_status_current
      - name: vehicle_status_history
      - name: vehicle_position
