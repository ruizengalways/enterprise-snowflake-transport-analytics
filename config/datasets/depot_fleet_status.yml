schema_version: 2

dataset:
  id: depot_fleet_status
  owner_team: transport-data
  materialization:
    type: dynamic_table
    target_lag: 5 minutes
    refresh_mode: adaptive
  runtime:
    mode: snowflake_managed
  compute:
    workload: transform
