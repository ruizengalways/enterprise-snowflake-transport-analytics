name: dbt Static CI

on:
  pull_request:
    paths:
      - "dbt/**"
      - "config/**"
      - "contracts/**"
      - ".github/workflows/dbt-static-ci.yml"
  push:
    branches:
      - main
    paths:
      - "dbt/**"
      - "config/**"
      - "contracts/**"
      - ".github/workflows/dbt-static-ci.yml"

permissions:
  contents: read

jobs:
  parse:
    name: Validate dbt project
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false

      - name: Parse with pinned framework
        uses: ruizengalways/enterprise-snowflake-data-project-framework/.github/actions/dbt-static-check@2c3de742c2f9b3ca3dee3f7a84533b80350c1b7c
        with:
          project-code: TRANSPORT
          project-dir: dbt
          project-root: .
