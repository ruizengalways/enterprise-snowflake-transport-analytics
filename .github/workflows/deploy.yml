name: Deploy
run-name: Deploy TRANSPORT ${{ github.sha }} to ${{ inputs.environment }}

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target Snowflake environment. Run this workflow from main."
        required: true
        type: choice
        options:
          - dev
          - uat
          - prod

permissions:
  contents: read
  id-token: write

concurrency:
  group: deploy-transport-${{ inputs.environment }}
  cancel-in-progress: false

jobs:
  deploy:
    uses: ruizengalways/enterprise-snowflake-data-project-framework/.github/workflows/project-deploy.yml@8afe208bd911a59b9334add78a53878ffea93087
    with:
      project_code: TRANSPORT
      environment: ${{ inputs.environment }}
      git_sha: ${{ github.sha }}
      framework_ref: 8afe208bd911a59b9334add78a53878ffea93087
      project_dir: dbt
